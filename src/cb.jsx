const Button = () => {
    return (
        <button
            className="prettier-class"
            id="prettier-id"
            onClick={this.handleClick}>
            Click Hereede
        </button>
    );
};

export { Button };
